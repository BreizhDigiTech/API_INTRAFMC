type Order {
  id: ID!
  user: User!
  total: Float!
  status: String!
  products: [OrderProduct!]!
  created_at: DateTime
  updated_at: DateTime
}

type OrderProduct {
  id: ID!
  name: String!
  price: Float!
  pivot: OrderProductPivot
}

type OrderProductPivot {
  quantity: Int!
  unit_price: Float!
}

input UpdateOrderStatusInput {
  id: ID!
  status: String!
}

extend type Mutation {
  checkout: Order
    @guard
    @field(resolver: "App\\Modules\\Order\\GraphQL\\Mutations\\OrderMutator@checkout")

  cancelOrder(id: ID!): Boolean
    @guard
    @field(resolver: "App\\Modules\\Order\\GraphQL\\Mutations\\OrderMutator@cancelOrder")

  updateOrderStatus(input: UpdateOrderStatusInput!): Order
    @guard
    @field(resolver: "App\\Modules\\Order\\GraphQL\\Mutations\\OrderMutator@updateOrderStatus")
}

extend type Query {
  # Toutes les commandes - Admin uniquement
  orders(first: Int, page: Int): [Order!]!
    @guard
    @can(ability: "viewAny", model: "App\\Models\\Order")
    @paginate(defaultCount: 10, maxCount: 50, model: "App\\Models\\Order")

  # Mes commandes - Utilisateur connecté
  myOrders(first: Int, page: Int): [Order!]!
    @guard
    @field(resolver: "App\\Modules\\Order\\GraphQL\\Queries\\OrderQuery@myOrders")

  # Détail d'une commande - Avec vérification de propriété
  order(id: ID!): Order
    @guard
    @field(resolver: "App\\Modules\\Order\\GraphQL\\Queries\\OrderQuery@order")
}